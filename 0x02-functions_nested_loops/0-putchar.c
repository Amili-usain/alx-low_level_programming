#include"main.h"
/**
 * main - Prints _putchar, followed by a new line.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char str[] = "_putchar\n";
	int i = 0;

	while (str[] != '\n')
	{
		char c = str[i];

		_putchar(c);
		i++;
	}
	return (0);
}
